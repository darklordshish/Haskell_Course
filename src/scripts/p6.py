#!/usr/bin/env python3
"""Pass-1 prose: expand Section 6 (Stochastic DSL / Bayesian)."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# 6. Стохастическое Программирование: DSL

## Мотивация

Имея монаду `Dist`, хочется **описывать генеративные модели как обычные программы**:
«сначала облачность, от неё зависит дождь и поливалка, от них — мокрая трава». А затем —
по наблюдению (трава мокрая) считать апостериорные вероятности причин.

## Идея и категорная конструкция

Стохастическая программа — это морфизм в `Kleisli(Dist)`, собранный в `do`-нотации.
Байесовское обновление — это **дезинтеграция** (disintegration) совместного распределения:
из `P(причина, наблюдение)` извлекается условное `P(причина | наблюдение)`.
Схема: $\text{Prior} \times \text{Likelihood} \propto \text{Posterior}$.

## Формализм

Правило Байеса:
$$P(H \mid E) = \frac{P(E \mid H)\,P(H)}{\sum_{H'} P(E \mid H')\,P(H')}.$$
Сеть «поливалки»: $\text{Cloudy} \to \{\text{Sprinkler}, \text{Rain}\} \to \text{WetGrass}$.
`do`-нотация строит **совместное** распределение, а фильтрация по наблюдению + нормировка
дают апостериорное.

## Код и демонстрация

В ячейке — байесовская сеть «поливалки» на `Dist`. Связывание в `do` сэмплирует совместное
распределение всех узлов; ограничение на наблюдаемый узел и нормировка реализуют
обусловливание. Так считается, например, $P(\text{Rain} \mid \text{WetGrass})$.

## Пример из жизни

Медицинская диагностика: по симптому (`E`) — апостериорное распределение болезней (`H`).
Спам-фильтр: по словам письма — вероятность «спам». Везде один приём: prior + правдоподобие
→ posterior.

## Границы применимости

Точный вывод **экспоненциален по ширине сети** (перебирает совместное распределение): годится
для небольших дискретных сетей, а на больших нужны приближённые методы (сэмплирование MCMC,
вариационный вывод). Требуются заданные таблицы условных вероятностей (CPT) — модель не
возникает из данных сама.

## Мостик

Если неопределённость **разворачивается во времени** и зависит только от текущего состояния —
это марковские цепи.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# 6. "):
            c["source"] = MD.splitlines(keepends=True); n += 1
    if n != 1:
        print(f"ERROR: matched {n} cells for section 6", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched sec6")


if __name__ == "__main__":
    main()
