#!/usr/bin/env python3
"""Pass-1 prose: expand Section 8 (Stochastic Matrices & Measure)."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# 8. Стохастические Матрицы и Теория Меры

## Мотивация

Разделы 2–7 — это разные ракурсы одной картины. Соберём их под общий **категорный и
теоретико-мерный** взгляд: что общего у распределений, ядер и марковских цепей и какие
инварианты их измеряют.

## Идея и категорная конструкция

Три категории выстраиваются в цепочку «всё точнее про вероятность»:

| Категория | Объекты | Морфизмы |
|---|---|---|
| `Hask` | типы | чистые функции |
| `Kleisli(Dist)` | типы | `a -> Dist b` (стох. отображения) |
| `FinStoch` | конечные множества | стохастические матрицы |

Связующая нить — **монада** $(Dist, \mathrm{return}, \mathrm{join})$. Умножение монады
`join : Dist (Dist a) -> Dist a` — это **маргинализация** распределения распределений (смесь).

## Формализм

$$\mathrm{join}\;(\textstyle\sum_i p_i\,[\,\textstyle\sum_j q_{ij}\,x_{ij}\,])
 = \textstyle\sum_{i,j} p_i q_{ij}\,x_{ij}.$$
Естественные инварианты на этих структурах:
$$H(p) = -\textstyle\sum_x p(x)\log p(x), \qquad
  D_{\mathrm{KL}}(p \Vert q) = \textstyle\sum_x p(x)\log\frac{p(x)}{q(x)}.$$
Энтропия $H$ измеряет неопределённость распределения, дивергенция $D_{\mathrm{KL}}$ — «расстояние»
от `p` до эталона `q`.

## Код и демонстрация

`joinDist` реализует умножение монады — схлопывает `Dist (Dist a)` в `Dist a`, перемножая
внешние и внутренние веса. Это и есть «смесь распределений» = маргинализация скрытой переменной.

## Пример из жизни

Пропускная способность канала связи и потери информации измеряются энтропией; рассогласование
модели и данных — через $D_{\mathrm{KL}}$ (основа кросс-энтропийной функции потерь в ML).

## Границы применимости

Картина — для **конечных** стохастических матриц; на непрерывных пространствах нужны плотности и
интегралы. $D_{\mathrm{KL}}$ зависит от эталона `q` и **не определена**, когда носители не
согласованы ($q(x)=0$ при $p(x)>0$); это не метрика (несимметрична).

## Мостик

А если само вероятностное пространство **нестабильно** — вероятности элементарных событий
меняются, и закон больших чисел не на что опереть? Тогда уместнее решёточная альтернатива —
теория возможностей Пытьева.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# 8. "):
            c["source"] = MD.splitlines(keepends=True); n += 1
    if n != 1:
        print(f"ERROR: matched {n} cells for section 8", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched sec8")


if __name__ == "__main__":
    main()
