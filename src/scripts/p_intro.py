#!/usr/bin/env python3
"""Pass-1 prose: expand intro framing cell."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# ❓ Uncertainty & Randomness in Haskell

> Категорное представление неопределённости и случайности: от типов-обёрток до монад Гири,
> модальных логик, нечёткой математики, стохастического программирования и теории возможностей.

**Сквозная идея ноутбука.** Неопределённость — это не одно понятие, а семейство, и каждый его
вид естественно выражается одной и той же категорной конструкцией: *поднять вычисление над
неизвестностью* — будь то **эндофунктор**, **монада** или **обогащение** в подходящей
категории. Мы идём по нарастанию структуры:

1. **качественные обёртки** (`Maybe`/`Either`/список) — «есть/нет/много», без меры;
2. **вероятность** (`Dist`, Гири) — взвешенные исходы, аддитивная алгебра $(+,\times)$;
3. **градуированная истинность и модальность** (нечёткость, Крипке) — степени и «возможные миры»;
4. **возможность** (Пытьев) — решёточная альтернатива $([0,1],\max,\min)$, когда само
   вероятностное пространство нестабильно.

| № | Тема |
|---|------|
| 1 | Неопределённость как функтор |
| 2 | Монада дискретных распределений |
| 3 | Монада Гири и вероятностные меры |
| 4 | Нечёткая математика и нечёткие множества |
| 5 | Модальные логики и фреймы Крипке |
| 6 | Стохастическое программирование: DSL для байесовских сетей |
| 7 | Марковские цепи в категории Клейсли |
| 8 | Категорная картина: стохастические матрицы и теория меры |
| 9 | Теория возможностей Пытьева |

> **Как читать.** Каждый раздел построен по единой схеме: *мотивация → идея и категорный смысл →
> формализм → код и демонстрация → пример из жизни → границы применимости → мостик к следующему
> разделу*. Итоговая сводка сводит все конструкции в одну таблицу и диаграмму.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# ❓"):
            c["source"] = MD.splitlines(keepends=True); n += 1
    if n != 1:
        print(f"ERROR: matched {n} intro cells", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched intro")


if __name__ == "__main__":
    main()
