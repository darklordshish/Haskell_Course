#!/usr/bin/env python3
"""Phase 11 Pass-1: reskeleton existing sections (batch 1) + renumber."""
import json, sys
PATH = "/home/jovyan/src/notebooks/Optics.ipynb"

SEC = {
"ee4848b0": r"""## 1️⃣ Линза через геттер/сеттер

## Мотивация

Обновить поле глубоко внутри неизменяемой структуры в «голом» Haskell больно:
`r { a = (a r) { b = ... } }`. Линза упаковывает пару «прочитать поле / записать поле» в
**один компонуемый объект**, и доступ к вложенным данным становится выразительным.

## Идея и категорный смысл

**Линза** `Lens s a` фокусируется ровно на одном значении `a` внутри `s`: она знает, как
его *достать* (`get :: s -> a`) и как *положить обратно* (`set :: s -> a -> s`). Категорно
линза — это коалгебра комонады `Store` (см. раздел 15).

## Формализм

Конкретное представление — запись из геттера и сеттера:
$$\mathrm{Lens}\,s\,a \;\approx\; (s \to a)\;\times\;(s \to a \to s).$$
Корректная линза подчиняется трём законам (раздел 4): get-put, put-get, put-put.

## Код и демонстрация

В ячейке — `CLens` (конкретная линза-запись) с `cget`/`cset` и пример фокуса на поле.
Это «учебное» представление; стандартное Haskell-кодирование (Ван Лааровен) — в разделе 2.

## Пример из жизни

Доступ к полю конфигурации, координате точки, балансу счёта — везде, где нужно и читать,
и менять одно значение во вложенной записи.

## Границы применимости

Линза требует, чтобы фокус **всегда существовал** (ровно одна цель). Для «может не быть»
нужна призма/AffineTraversal, для «много целей» — обход.

## Мостик

Запись-пара неуклюже композируется. Элегантная композиция приходит с кодированием
Ван Лааровена.
""",

"eac66bac": r"""## 2️⃣ Линза Ван Лааровена

## Мотивация

Хочется, чтобы линзы **компоновались как обычные функции** и не плодили обёрток. Решение —
кодирование Ван Лааровена через функторы высшего ранга.

## Идея и категорный смысл

Линза представляется одной полиморфной функцией:
выбор функтора `f` определяет операцию. `Identity` даёт `over`/`set`, `Const` даёт `view`.

```haskell
type Lens s a = forall f. Functor f => (a -> f a) -> s -> f s
```

## Формализм

$$\mathrm{Lens}\,s\,a = \forall f.\ \mathrm{Functor}\,f \Rightarrow (a \to f\,a) \to (s \to f\,s).$$
`view l = getConst . l Const`, `over l g = runIdentity . l (Identity . g)`.

## Код и демонстрация

В ячейке — `Identity`/`Const` и реализация `view`/`over`/`set` через один `Lens`. Один и
тот же `l` работает и на чтение, и на запись — функтор «переключает режим».

## Пример из жизни

Это представление лежит в основе библиотеки `lens`: им пользуются миллионы строк кода.

## Границы применимости

Нужен `RankNTypes`. Тип выглядит пугающе, зато композиция тривиальна (раздел 3) и
покрывает не только линзы, но и все оптики (раздел 12).

## Мостик

Главный бонус кодирования — композиция линз как `(.)`.
""",

"4fd0b3c1": r"""## 3️⃣ Композиция линз = композиция функций

## Мотивация

Главная причина любить оптики: путь `company.dept.head.salary` собирается **композицией**
маленьких линз — без ручного разбора вложенности.

## Идея и категорный смысл

В кодировании Ван Лааровена линза — это функция, поэтому композиция линз — это буквально
композиция функций `(.)`. Читается «снаружи внутрь»: `outer . inner`.

## Формализм

Если `l1 :: Lens a b` и `l2 :: Lens b c`, то `l1 . l2 :: Lens a c`. Ассоциативность и
единица (`id`-линза = `Iso`) делают линзы категорией.

## Код и демонстрация

В ячейке — две линзы и их композиция; `view`/`over` через составную линзу достают и меняют
вложенное поле одним выражением. Полноценный практический пример — в разделе 18.

## Пример из жизни

`over (deptL . headL . salaryL) (+15000)` — повышение зарплаты руководителя отдела одной
строкой.

## Границы применимости

Композиция «смешивает» виды оптик: Lens∘Lens=Lens, но Lens∘Prism=AffineTraversal и т.д. —
тип результата определяется решёткой мощности (раздел 19).

## Мостик

Прежде чем доверять линзам, убедимся, что они **корректны** — законы линз.
""",

"8453f15d": r"""## 5️⃣ Призма — оптика для типов-сумм

## Мотивация

Линза фокусируется на поле произведения; призма — двойственно — на **одной ветви суммы**
(один конструктор). Фокус может *отсутствовать* (значение — другая ветвь).

## Идея и категорный смысл

**Призма** `Prism s a` умеет: `preview :: s -> Maybe a` (попробовать достать) и
`review :: a -> s` (построить). Категорно призма — это `Choice`-профунктор (раздел 12),
двойственный `Strong` у линзы.

## Формализм

$$\mathrm{Prism}\,s\,a \;\approx\; (s \to \mathrm{Maybe}\,a)\;\times\;(a \to s),$$
с законом `preview . review = Just` (построил — значит достанешь обратно).

## Код и демонстрация

В ячейке — `CPrism` с `previewP`; ниже отдельная ячейка показывает **review/re**
(построение) на `_Just`/`_Left`. Призма = «частичный геттер + умный конструктор».

## Пример из жизни

Парсинг и умные конструкторы: `_Integer :: Prism String Integer` — распарсить строку или
собрать её обратно из числа; разбор тегированных сообщений по конструктору.

## Границы применимости

Фокус 0-или-1 **в сумме**; для 0-или-1 в произвольной структуре — AffineTraversal (раздел 7).
`review` существует не у всех оптик — это особенность призм/изо.

## Мостик

Полностью обратимый случай (всегда ровно одна цель, туда и обратно) — это изо.
""",

"74aa2e15": r"""## 6️⃣ Изо — изоморфизмы как оптики

## Мотивация

Иногда два типа — это **одно и то же под разными именами** (`(a,b)` и запись из двух полей,
`Celsius` и `Kelvin`). Изо выражает такой изоморфизм как оптику.

## Идея и категорный смысл

**Изо** `Iso s a` говорит «`s` и `a` изоморфны»: пара `from :: s -> a`, `to :: a -> s` с
`from . to = id`, `to . from = id`. Изо — самая мощная оптика: она одновременно и линза,
и призма.

## Формализм

$$\mathrm{Iso}\,s\,a \;\approx\; (s \to a)\;\times\;(a \to s),\quad \text{взаимно обратные}.$$

## Код и демонстрация

В ячейке — `CIso` с `forward`/`backward` и операции `view`/`review` через изо. Любой изо
тривиально «ослабляется» до линзы или призмы.

## Пример из жизни

Единицы измерения, кодеки (`ByteString ↔ Text`), `swap` для пар, представление `newtype`.

## Границы применимости

Изо требует **настоящей** обратимости: если преобразование теряет информацию, это не изо,
а лишь геттер/линза.

## Мостик

Ослабим требование «ровно одна цель» до «ноль или одна» — AffineTraversal.
""",

"51067c07": r"""## 8️⃣ Обход: доступ к нескольким значениям

## Мотивация

Линза/призма фокусируются на 0–1 цели. Но часто нужно «пройтись по **всем** элементам»:
обновить каждый, собрать список, выполнить эффект для каждого.

## Идея и категорный смысл

**Traversal** `Traversal s a` фокусируется на *нескольких* значениях. В кодировании
Ван Лааровена он обобщает линзу с `Functor` до `Applicative` — именно `Applicative`
позволяет «накапливать» эффекты по многим целям.

## Формализм

$$\mathrm{Traversal}\,s\,a = \forall f.\ \mathrm{Applicative}\,f \Rightarrow (a \to f\,a) \to (s \to f\,s).$$
`Lens` — частный случай при `Functor` (ровно одна цель).

## Код и демонстрация

В ячейке — обход, применяющий функцию ко всем элементам структуры (`over`), и сбор всех
значений (`toListOf`). `Applicative` склеивает результаты по всем фокусам.

## Пример из жизни

Прибавить налог ко всем позициям заказа; собрать все email из дерева; провалидировать
каждый элемент формы с накоплением ошибок.

## Границы применимости

Число целей 0..n; порядок обхода задаётся структурой. Read-only-частный случай — Fold
(раздел 9).

## Мостик

Если из обхода убрать запись — получим Fold/Getter; если оставить только запись — Setter.
""",
}

def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    by_id = {c.get("id"): c for c in nb["cells"]}
    n = 0
    for cid, text in SEC.items():
        c = by_id.get(cid)
        if c is None:
            print(f"ERROR missing {cid}", file=sys.stderr); sys.exit(1)
        c["source"] = text.splitlines(keepends=True); n += 1
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print(f"reskeleton batch1: {n} sections")

if __name__ == "__main__":
    main()
