#!/usr/bin/env python3
"""Pass-1 prose: expand Section 1 (Uncertainty as Functor)."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# 1. Неопределённость как Функтор

## Мотивация

Самая первая, ещё не количественная неопределённость — это «значение, **возможно**, есть».
Вычисление может не вернуть результат (деление на ноль), вернуть ошибку (неверный ввод) или
сразу **много** результатов (перебор вариантов). В чистом функциональном программировании это
кодируется **в типе**, а не выбрасывается как исключение: тип честно сообщает, что выход
неопределён, и компилятор заставляет обработать каждый случай.

## Идея и категорная конструкция

Каждый такой «контейнер неопределённости» — это **эндофунктор на категории `Hask`**
(объекты — типы, морфизмы — функции). Функтор `F` поднимает чистое вычисление `f : a -> b`
до вычисления над обёрткой через `fmap : (a -> b) -> F a -> F b`, не трогая саму «оболочку
неопределённости». Это и есть смысл функториальности: *структура неизвестности сохраняется,
меняется только полезная нагрузка*.

| Тип | Что моделирует | Алгебра |
|---|---|---|
| `Maybe a` | отсутствие/наличие значения | $1 + a$ |
| `Either e a` | ошибка `e` либо успех `a` | $e + a$ |
| `[a]` | недетерминизм, множество исходов | свободный моноид |
| `Validation e a` | **накопление** всех ошибок сразу | $e + a$, $e$ — полугруппа |

## Формализм

Функториальные законы (их выполнение и делает обёртку функтором):
$$fmap\; \mathrm{id} = \mathrm{id}, \qquad fmap\,(g \circ f) = fmap\,g \circ fmap\,f.$$
Ключевое отличие `Validation` от `Either`: его `Applicative`-инстанс при двух ошибках
**склеивает** их через `Semigroup e` ($e_1 <> e_2$), тогда как `Either` останавливается на первой.

## Код и демонстрация

В ячейке ниже `safeDiv` иллюстрирует `Maybe`, `parseAge` — `Either` с диагностикой,
блок `Validation` показывает накопление `["e1","e2"]`, а списковая часть — недетерминизм
(декартово произведение `3x3`). Каждый пример — отдельный функтор неопределённости.

## Пример из жизни

Валидация формы регистрации: с `Either` пользователь узнаёт об ошибках по одной
(исправил поле — всплыла следующая), а с `Validation` получает **сразу весь список**
проблем за один проход. Один и тот же тип-сумма, разная аппликативная семантика — разный UX.

## Границы применимости

Функторы организуют вычисление над неопределённостью, но **не дают количественной меры**:
нет вероятностей, степеней, весов — только «есть / нет / много». Нельзя спросить «насколько
вероятен исход» или «какой вариант правдоподобнее». Как только нужна *мера*, одной обёртки мало.

## Мостик

Чтобы **взвесить** исходы и считать вероятности — переходим от структуры-обёртки к
распределению: монаде дискретных вероятностей `Dist`.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# 1. "):
            c["source"] = MD.splitlines(keepends=True)
            n += 1
    if n != 1:
        print(f"ERROR: matched {n} cells for section 1", file=sys.stderr)
        sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1)
        f.write("\n")
    print("patched sec1")


if __name__ == "__main__":
    main()
