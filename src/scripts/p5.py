#!/usr/bin/env python3
"""Pass-1 prose: expand Section 5 (Modal Logics)."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# 5. Модальные Логики

## Мотивация

Иногда важно рассуждать не «насколько вероятно», а «**необходимо** ли» утверждение и
«**возможно** ли» оно — в разных мыслимых положениях дел. Это неопределённость о том, какой
из «возможных миров» реализуется, и язык для неё — модальная логика.

## Идея и категорная конструкция

Модель — **фрейм Крипке** $(W, R)$: множество миров `W` и отношение достижимости `R`.
- $\Box p$ («необходимо `p`») истинно в мире, если `p` верно во **всех** достижимых мирах;
- $\Diamond p$ («возможно `p`») — если `p` верно **хотя бы в одном**.

Категорно: $\Box$ ведёт себя как **правый сопряжённый** (сохраняет пределы/конъюнкции), а
$\Diamond$ — как левый сопряжённый (сохраняет копределы/дизъюнкции).

## Формализм

$$w \models \Box p \iff \forall v.\; wRv \Rightarrow v \models p,
\qquad w \models \Diamond p \iff \exists v.\; wRv \wedge v \models p.$$
Условия на `R` задают систему аксиом:

| Система | Характеристическая аксиома | Условие на R |
|---|---|---|
| S4 | $\Box p \to \Box\Box p$ | транзитивность |
| S5 | $\Diamond p \to \Box\Diamond p$ | отношение эквивалентности |

## Код и демонстрация

`KripkeFrame` хранит миры и функцию достижимости `access`. Операторы `Box`/`Diamond`
вычисляются перебором достижимых миров: `Box` требует истинности всюду, `Diamond` —
хотя бы где-то. Меняя `access`, получаем разные модальные системы.

## Пример из жизни

Эпистемическая логика: $\Box p$ = «агент **знает** `p`» (во всех мирах, неотличимых для него,
`p` верно). Темпоральная логика: $\Box$ = «всегда», $\Diamond$ = «когда-нибудь». Так
формализуют знание, обязательства, время.

## Границы применимости

Модальность **качественная**: степеней нет, только «во всех / хотя бы в одном». Разные условия
на `R` дают разные логики — аксиомы надо подбирать под предметную область, они не выводятся
сами. Комбинировать численные свидетельства (как в Байесе) здесь нельзя.

## Мостик

Чтобы **декларативно программировать** вероятностные модели и считать апостериори —
стохастический DSL для байесовских сетей.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# 5. "):
            c["source"] = MD.splitlines(keepends=True); n += 1
    if n != 1:
        print(f"ERROR: matched {n} cells for section 5", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched sec5")


if __name__ == "__main__":
    main()
