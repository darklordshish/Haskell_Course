#!/usr/bin/env python3
"""Pass-1 prose: expand Section 2 (Discrete Distribution Monad)."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""# 2. Монада Дискретных Распределений

## Мотивация

Функтор говорит «исходов несколько», но молчит о том, **какой насколько вероятен**. Чтобы
взвесить исходы, добавим к каждому из них число — вероятность. Так появляется конечное
дискретное распределение `Dist a`: список пар «значение — вес» с нормировкой $\sum p_i = 1$.

## Идея и категорная конструкция

`Dist` — это **монада**. Её категория Клейсли `Kleisli(Dist)` изоморфна **FinStoch**:
объекты — типы (конечные множества исходов), морфизмы `a -> Dist b` — *стохастические
отображения* (марковские ядра на конечных множествах). При этом:

- `return x` — точечная масса (вероятность 1 на `x`);
- `>>=` — **закон полной вероятности**: композиция стохастических отображений.

## Формализм

$$Dist\,a = \{(x_i, p_i) \mid p_i \ge 0,\ \textstyle\sum_i p_i = 1\}.$$
Связывание маргинализует промежуточную переменную:
$$(m \mathbin{>\!\!>\!\!=} k)(y) = \sum_{x} m(x)\, k(x)(y).$$
Это ровно умножение стохастических матриц — потому Клейсли-категория и есть FinStoch.

## Код и демонстрация

В ячейке определены `Monad`-инстанс для `Dist` и демонстрация: композиция бросков,
обновление весов и нормировка. Обрати внимание, что `>>=` **сворачивает** двухшаговый
эксперимент в одно распределение по финальному исходу — это и есть маргинализация.

## Пример из жизни

Бросок кубика, затем — в зависимости от грани — вытягивание карты: `>>=` автоматически
даёт итоговое распределение по картам. Любые цепочки «исход влияет на следующий шаг»
выражаются естественно, без ручного перемножения вероятностей.

## Границы применимости

`Dist` работает только с **конечным носителем**: непрерывные величины (рост, время) так не
описать — для них нужна теория меры (раздел 3). Здесь ещё нет синтаксиса *обусловливания*
(он появится в разделе 6). Веса хранятся в `Double` и накапливают ошибку округления; точный
счёт потребовал бы рациональных чисел.

## Мостик

Чтобы работать с бесконечными и непрерывными пространствами, обобщим распределение до
**меры** — это монада Гири.
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("# 2. "):
            c["source"] = MD.splitlines(keepends=True)
            n += 1
    if n != 1:
        print(f"ERROR: matched {n} cells for section 2", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched sec2")


if __name__ == "__main__":
    main()
