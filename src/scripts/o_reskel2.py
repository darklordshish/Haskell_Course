#!/usr/bin/env python3
"""Phase 11 Pass-1: reskeleton existing sections (batch 2) + intro + contents."""
import json, sys
PATH = "/home/jovyan/src/notebooks/Optics.ipynb"

SEC = {
"b5e30e1a": r"""## 1️⃣2️⃣ Унификация через профункторы

## Мотивация

У каждой оптики своё конкретное представление — это разрозненно. Профункторное кодирование
даёт **единый тип** для всех оптик сразу, и тогда композиция и переиспользование становятся
бесплатными.

## Идея и категорный смысл

Оптика — это естественное преобразование `forall p. C p => p a b -> p s t`, где класс-
ограничение `C` задаёт *вид* оптики: `Strong` → Lens, `Choice` → Prism, `Strong`+`Choice`
→ AffineTraversal, `Applicative`/`Traversing` → Traversal, `Closed` → Grate.

## Формализм

$$\mathrm{Optic}_C\,s\,t\,a\,b = \forall p.\ C\,p \Rightarrow p\,a\,b \to p\,s\,t.$$
`dimap :: (a'->a) -> (b->b') -> p a b -> p a' b'` — база профунктора; `first'`/`left'`
добавляют произведение/сумму (Strong/Choice).

## Код и демонстрация

В ячейке — локальный класс `Profunctor` и реализация линзы/призмы через `Strong`/`Choice`.
Тот же `p`-параметр объединяет все оптики под одной сигнатурой.

## Пример из жизни

Это представление лежит в основе пакета `optics` и продвинутых частей `lens`: оно даёт
«оптики как одну абстракцию».

## Границы применимости

Требует нескольких расширений типов; абстрактнее VL-кодирования, зато максимально единообразно.

## Мостик

Самый необычный профунктор-класс — `Closed` — порождает оптику для коэкспоненциалов: Grate.
""",

"5fa12c01": r"""## 1️⃣3️⃣ Grate: оптика для коэкспоненциалов

## Мотивация

Линза работает с произведением, призма — с суммой. **Grate** работает с *функциональным*
(экспоненциальным) фокусом — «много значений, индексированных входом».

## Идея и категорный смысл

Grate строится на профункторе `Closed` (метод `closed :: p a b -> p (x->a) (x->b)`).
Она позволяет «зайти под стрелку `x -> _`» и преобразовать результат.

## Формализм

$$\mathrm{Grate}\,s\,a \;\approx\; ((s \to a) \to b) \to t \quad(\text{через } \mathrm{Closed}).$$

## Код и демонстрация

В ячейке — локальный `Closed`-профунктор и пример grate над функциональным типом
(`zipWithOf`-подобное поведение). Grate раздаёт операцию по всем «выходам» функции.

## Пример из жизни

Покомпонентные операции над функциями/таблицами: применить преобразование к каждому
значению `x -> a`, представлению сигнала, вектору как функции индекса.

## Границы применимости

Менее распространена, чем Lens/Prism/Traversal; нужна, когда фокус по природе
экспоненциальный (функция), а не произведение/сумма.

## Мостик

Все рассмотренные оптики выражаются единым профунктор-интерфейсом — соберём это явно.
""",

"4e5812c4": r"""## 1️⃣4️⃣ Все оптики через профунктор-интерфейс

## Мотивация

Подытожим унификацию: показать, что каждая оптика — это выбор класса-ограничения над одной
и той же сигнатурой, и что иерархия `Functor`/`Applicative` тоже укладывается в эту картину.

## Идея и категорный смысл

`Optic_C s t a b = forall p. C p => p a b -> p s t`. Усиливая `C`, мы спускаемся по решётке
мощности: `Iso` (только `Profunctor`) ⊃ `Lens` (`Strong`) / `Prism` (`Choice`) ⊃
`Traversal` (`Traversing`) ⊃ `Fold`/`Setter`.

## Формализм

Через `Const`/`Identity` профункторы из VL-кодирования восстанавливаются `view`/`over`;
через `Star f` — обходы для `Applicative f`. Класс-ограничение = вид оптики.

## Код и демонстрация

В ячейке — представление иерархии через `Functor`/`Applicative` (`ConstApp`) и то, как
ослабление/усиление ограничения меняет доступную силу оптики.

## Пример из жизни

Библиотеки `lens`/`optics` именно так дают «один комбинатор работает для всех оптик».

## Границы применимости

Единый интерфейс — ценой абстрактности типов; в учебных целях полезно держать рядом и
конкретные представления (разделы 1–11).

## Мостик

За практикой стоит красивая теория: линза — это коалгебра комонады `Store`.
""",

"b6e0bcce": r"""## 1️⃣6️⃣ Оптики и `Traversable`

## Мотивация

Класс `Traversable` из [FoldableTraversable.ipynb](FoldableTraversable.ipynb) и обходы-
оптики — про одно и то же: пройти структуру с эффектом. Свяжем их явно.

## Идея и категорный смысл

`traverse :: Applicative f => (a -> f b) -> t a -> f (t b)` — это и есть `Traversal (t a) a`
(с точностью до типов). Любой `Traversable`-контейнер даёт «бесплатный» обход `traversed`.

## Формализм

$$\mathrm{traverse} \;:\; (a \to f\,b) \to t\,a \to f\,(t\,b) \;\equiv\; \mathrm{Traversal}\,(t\,a)\,(t\,b)\,a\,b.$$

## Код и демонстрация

В ячейке — связь `traverse`/`sequenceA` с обходом-оптикой: `toListOf traversed`,
модификация всех элементов, накопление эффектов через `Applicative`.

## Пример из жизни

Любая структура-контейнер (дерево, `Map`, `[]`) автоматически получает обход всех листьев.

## Границы применимости

`traversed` обходит «канонические» элементы контейнера; для нестандартного фокуса нужен
ручной `Traversal`.

## Мостик

На практике всё это упаковано в библиотеки `lens` и `optics`.
""",

"4b621ada": r"""## 1️⃣7️⃣ Библиотеки `lens` и `optics`

## Мотивация

В реальном коде оптики берут из готовых библиотек. Полезно знать, что они дают и чем
отличаются.

## Идея

**`lens`** (Edward Kmett) — наиболее полная реализация на VL-кодировании: тысячи
комбинаторов, операторы (`^.`, `.~`, `%~`, `^..`), `makeLenses` через Template Haskell.
**`optics`** — альтернатива на профунктор/абстрактном представлении: лучшие сообщения об
ошибках и явные виды оптик (тип `Optic`), ценой иной композиции (`%` вместо `.`).

## Псевдонимы и ключевые функции

`view`/`^.`, `set`/`.~`, `over`/`%~`, `preview`/`^?`, `toListOf`/`^..`, `review`/`#`.
Линзы строятся `makeLenses`, призмы — `makePrisms`.

## Границы применимости

Среда IHaskell здесь **не** несёт эти пакеты — в ноутбуке используются локальные учебные
реализации. В проде же берут `lens`/`optics`, а не пишут VL-машинерию руками.

## Мостик

Закрепим всё развёрнутым практическим примером — глубоким апдейтом вложенной записи.
""",

"d1452bd1": r"""# 🔍 Оптики в Haskell

**Оптики** — это компонуемые, переиспользуемые инструменты доступа и модификации частей
структуры данных: прочитать, записать, обновить произвольно глубоко вложенное значение
**одним выражением**, не разбирая вложенность руками.

**Сквозная идея ноутбука.** Все оптики — это естественные преобразования
`forall p. C p => p a b -> p s t`, где класс-ограничение `C` задаёт *вид* оптики. Поэтому
они **компонуются обычной `(.)`** и образуют **решётку по мощности**: от `Iso` (всё можно,
обратимо) вниз к специализированным `Fold`/`Setter`. Мы идём от конкретных представлений к
профункторной унификации и теоретической связи с комонадой `Store`.

> **Как читать.** Каждый содержательный раздел построен по единой схеме: *мотивация → идея и
> категорный смысл → формализм → код и демонстрация → пример из жизни → границы применимости →
> мостик*. Итоговая сводка сводит иерархию в постер, таблицу и абзац.
""",

"cmp16c0daf1": r"""## Содержание

| # | Раздел | Ключевая идея |
|---|--------|----------------|
| 1 | Линза через геттер/сеттер | фокус на поле произведения (get+set) |
| 2 | Линза Ван Лааровена | `forall f. Functor f => (a->f a)->s->f s` |
| 3 | Композиция линз | оптики компонуются как `(.)` |
| 4 | Законы линз | get-put, put-get, put-put |
| 5 | Призма (+ review/re) | фокус на ветви суммы; preview + build |
| 6 | Изо | изоморфизм `s` &#8596; `a` |
| 7 | AffineTraversal / Optional | фокус 0-или-1 |
| 8 | Обход (Traversal) | много целей через `Applicative` |
| 9 | Fold / Getter / Setter | read-only и write-only оптики |
| 10 | Индексированные оптики | обход с позицией/ключом |
| 11 | Контейнерные: Ix / At / Each | доступ/вставка/удаление по ключу |
| 12 | Унификация через профункторы | один тип для всех оптик |
| 13 | Grate | оптика коэкспоненциалов (`Closed`) |
| 14 | Все оптики через профунктор-интерфейс | класс-ограничение = вид оптики |
| 15 | Lens &#8596; комонада Store | линза = коалгебра Store; `Store ⊣ State` |
| 16 | Оптики и `Traversable` | `traverse` = обход-оптика |
| 17 | Библиотеки `lens` / `optics` | практические реализации |
| 18 | Практический пример | глубокий апдейт вложенной записи |
| 19 | Сводка: иерархия оптик | постер + таблица + текст |
""",
}

def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    by_id = {c.get("id"): c for c in nb["cells"]}
    n = 0
    for cid, text in SEC.items():
        c = by_id.get(cid)
        if c is None:
            print(f"ERROR missing {cid}", file=sys.stderr); sys.exit(1)
        c["source"] = text.splitlines(keepends=True); n += 1
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print(f"reskeleton batch2: {n} sections")

if __name__ == "__main__":
    main()
