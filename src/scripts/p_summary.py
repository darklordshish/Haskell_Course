#!/usr/bin/env python3
"""Pass-1 prose: rewrite summary with 3-channel categorical-constructions summary."""
import json, sys

PATH = "/home/jovyan/src/notebooks/Uncertainty.ipynb"

MD = r"""## Сводка

Мы рассмотрели девять структур описания неопределённости — от качественных обёрток до
решёточной теории возможностей. За внешним разнообразием стоит **один категорный приём**:
взять вид неопределённости и реализовать его как эндофунктор / монаду / обогащение в
подходящей категории, где определены *композиция* (как сцеплять шаги) и *измерение* (чем мерить
неизвестность).

### Категорные конструкции неопределённости

![Категорные конструкции неопределённости](../diagrams/uncertainty/unc_summary_poster.svg)

| Вид неопределённости | Категория / контекст | Эндофунктор / обогащение | Композиция | Измерение |
|---|---|---|---|---|
| `Maybe`/`Either`/`[]`/`Validation` | `Hask` | эндофунктор | `fmap` | — (нет меры) |
| `Dist` (дискретн.) | `Kleisli(Dist)` ≅ FinStoch | монада `Dist` | `>>=` (полная вероятн.) | $\sum p = 1$ |
| Гири | `Kleisli(G)` на Meas | монада `G` | интеграл по ядру | мера $\mu$ |
| Нечёткость | обогащение над $[0,1]$ | `X → [0,1]` | t-норма / t-конорма | степень $\mu$ |
| Модальность | фрейм Крипке $(W,R)$ | $\Box$ (прав. сопр.) / $\Diamond$ | по достижимости $R$ | качественно |
| Марков | эндо-`Kleisli(Dist)` | `S → Dist S` | Клейсли-степень | стац. $\pi P = \pi$ |
| Возможность (Пытьев) | решётка $L=([0,1],\max,\min)$ | $\tau: X \to [0,1]$, $\sup\tau = 1$ | $\max$ / $\min$ | $\Pi = \sup\tau$, $N = \inf\bar\tau$ |

### Единая картина

Каждая строка — **та же мысль** («поднять вычисление над неопределённостью»), реализованная в
своей категории. Меняются три вещи: *где живёт неопределённость* (категория), *как сцепляются
шаги* (композиция — `fmap`, `>>=`, интеграл, t-норма, $\max/\min$) и *чем она измеряется* (ничем,
вероятностью, мерой, степенью, возможностью). Вероятность — лучший инструмент, когда
вероятностное пространство **стабильно** (тогда работает закон больших чисел и частота стремится
к вероятности). Когда же оно **меняется**, естественнее решёточная мера возможности. Это не
вопрос «верной» теории, а вопрос **границ применимости**: каждая конструкция строга и
непротиворечива в своей области.

| № | Тема | Ключевая идея |
|---|------|---------------|
| 1 | Функторы неопределённости | `Maybe`, `Either`, список, `Validation` — структура без меры |
| 2 | Монада дискретных распределений | взвешенные исходы, `>>=` = полная вероятность |
| 3 | Монада Гири | марковские ядра, меры на пространстве, `>>=` = интеграл |
| 4 | Нечёткая математика | t-нормы, степени принадлежности, решётка $[0,1]$ |
| 5 | Модальные логики | операторы $\Box$/$\Diamond$, фреймы Крипке |
| 6 | Стохастическое программирование | байесовские сети, дезинтеграция, prior×likelihood |
| 7 | Марковские цепи | эндо-ядра в Клейсли, стационарное $\pi$ |
| 8 | Стохастические матрицы и мера | монада `Dist`, энтропия, дивергенция Кульбака–Лейблера |
| 9 | Теория возможностей Пытьева | $\Pi=\sup\tau$, $N=\inf\bar\tau$ — решёточная альтернатива вероятности |
"""


def main():
    with open(PATH, encoding="utf-8") as f:
        nb = json.load(f)
    n = 0
    for c in nb["cells"]:
        if c["cell_type"] == "markdown" and "".join(c["source"]).startswith("## Сводка"):
            c["source"] = MD.splitlines(keepends=True); n += 1
    if n != 1:
        print(f"ERROR: matched {n} summary cells", file=sys.stderr); sys.exit(1)
    with open(PATH, "w", encoding="utf-8") as f:
        json.dump(nb, f, ensure_ascii=False, indent=1); f.write("\n")
    print("patched summary")


if __name__ == "__main__":
    main()
