# Дизайн: ноутбук-словарь двойственности (Duality.ipynb)

**Дата:** 2026-06-16. **Статус:** согласовано в брейншторме.

## Цель

Отдельный ноутбук-справочник «парад двойственных категорных конструкций». Пользователь
дополняет его со временем. Организующая идея — **одна двойственность**: обращение стрелок
`C ⇄ C^op`. Подача — top-down: от расширений Кана (универсальная вершина) вниз к
пределам/сопряжениям, затем — примитивные op-дуалы (моно/эпи и т.п.).

## Ключевые решения (брейншторм)

- **Размещение:** отдельный `src/notebooks/Duality.ipynb`, вне линейного ствола 0–VI
  (приложение/справочник). В NAV-цепочку не вставляется; ссылки — из релевантных тем
  и из README (как «приложение»).
- **Единая ось — `C^op`.** Co-приставка = «то же в `C^op`». Lan/Ran — зеркальная пара
  под этой двойственностью (`(Lan_K F)^op ≅ Ran_{K^op}(F^op)`) и одновременно универсальная
  вершина, порождающая пределы и сопряжения. Моно/эпи — **примитивы** (обращённые стрелки),
  Каном не порождаются — это явно оговаривается (граница слогана «всё — расширение Кана»).
- **Глубина раздела:** полный — мат-определение + универсальное свойство + парная диаграмма
  (обращение стрелок/конуса) + Haskell обоих членов + строка «в чём двойственность» +
  ссылки «где в курсе».
- **Оглавление:** сводная таблица **вручную** в v1 (генератор — позже, если пар станет много).
- **v1:** каркас (мотивация + таблица + setup/NAV) + несколько наполненных разделов-образцов;
  при этом в плане прописан **скелет каждого раздела**, разделы независимы и возобновляемы.

## Структура ноутбука

0. **Мотивация** (крепкая): двойственность = обращение стрелок (`C^op`); приставка «co-»;
   Lan/Ran как зеркальная пара и вершина; слоган Маклейна и его граница (моно/эпи — вне).
1. **Вершина — расширения Кана:** `Lan` (левое) ↔ `Ran` (правое).
2. **Спуск (частные случаи Кана / op-дуалы):**
   - Сопряжения левое/правое (связь с Lan/Ran; левый сохраняет копределы, правый — пределы).
   - Пределы/копределы как Кан вдоль `J→1` (`lim=Ran`, `colim=Lan`) — **центральный приём**:
     форма-диаграмма `J` в центре, копредел слева (коконус), предел справа (конус).
   - Классические формы (частные `J`): терминальный/начальный (`J=∅`),
     произведение/копроизведение (`J`=дискретная пара), уравнитель/коуравнитель
     (`J`=параллельная пара), pullback/pushout (`J`=коспан/спан).
3. **Примитивы (op-дуалы вне Кана):** моно/эпи, ковариантный/контравариантный функтор,
   алгебры/коалгебры, монады/комонады.

Сверху — **сводная таблица** (вручную): `Конструкция | Двойственная | Идея | мини-диаграмма |
Haskell | где в курсе | →подробнее`.

## Шаблон раздела пары (скелет — единый для всех)

Каждый раздел самодостаточен и состоит из:
1. **Заголовок** с якорем (для ссылок из таблицы).
2. **Идея/мотивация** — 1–2 предложения, в чём пара и как одно получается из другого
   обращением стрелок (или зеркалом `C^op`).
3. **Мат-определение** обоих членов + **универсальное свойство**.
4. **Парная диаграмма** (SVG, канон Faza 6/Фазы B: светлая палитра, кит стрелок
   mono `↣`/epi `↠`/iso `↔`/nat `⇒`): левый член и правый член бок о бок, видно обращение
   стрелок/конуса. Для лимитной части — форма `J` в центре.
5. **Haskell** обоих членов (исполняемый код, IHaskell).
6. **«В чём двойственность»** — одна строка-резюме.
7. **«Где в курсе»** — ссылки на ноутбуки (pullback↔Toposes, Lan/Ran↔KanExtensions,
   монады↔Monads, алгебры↔AlgebrasCoalgebras и т.д.).

## Реализация: фрагментарность и возобновляемость

- План перечисляет **каждый раздел отдельным независимым шагом** со своим скелетом
  (что наполнять). Разделы не зависят друг от друга по исполнению.
- v1: собрать каркас (titre, мотивация, таблица, setup, NAV) + наполнить образцы
  (Lan/Ran-вершина, lim/colim с центральным приёмом, моно/эпи как примитив). Остальные
  разделы — заглушка-скелет (заголовок + якорь + «TODO по шаблону»), чтобы строка таблицы
  и ссылка уже работали, а наполнение шло потом без потери общей картины.
- Диаграммы рисуются по канону (`DesignShowcase.ipynb` + `normalize_palette.js`), ASCII-only
  в SVG, матсимволы через `&#NNNN;`, кит маркеров стрелок.

## Интеграция

- README/ROADMAP: упомянуть `Duality.ipynb` как справочник-приложение (вне ствола 0–VI).
- Из релевантных ноутбуков — ссылки на соответствующие разделы словаря (опционально, по мере
  наполнения).

## Верификация

- JSON-валидность ноутбука; `nbconvert --execute` в контейнере (0 ошибок) — для наполненных
  разделов с кодом.
- SVG-диаграммы: ASCII-only, палитра-канон (`normalize_palette.js` не меняет их), well-formed.
- Ссылки из таблицы ведут на существующие якоря; ссылки «где в курсе» — на существующие файлы.

## Вне объёма

- Генератор оглавления (позже).
- Встраивание в линейный NAV/course_order (это справочник, вне ствола).
- Полное наполнение всех разделов в v1 (наполняется фрагментарно со временем).
